Use 'make parser' to make.
Use './parser lexer.l parser.y program.prog' to parse and run the the specified lexer and parser on 'program.prog'.
Included are gram1.l, gram1.y, gram1.prog as a basic test.